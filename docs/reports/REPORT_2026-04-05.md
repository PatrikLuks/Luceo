# Luceo — Report 5. 4. 2026

**Session:** 1 (implementace backend MVP)
**Autor:** AI asistent (Claude) pod vedením Patrika Lukse
**Datum:** 5. dubna 2026

---

## Shrnutí

Session 1 navazuje na Session 0 (inicializace projektu, modely, API endpointy, crisis detection, guardrails). Hlavní cíle byly:
1. Implementace rate limitingu
2. Implementace refresh tokenů
3. Inicializace Alembic migrací
4. Kompletní audit a oprava nalezených problémů

**Všechny 4 cíle byly splněny.**

---

## Stav projektu

| Metrika | Před (Session 0) | Po (Session 1) | Změna |
|---|---|---|---|
| Testy | 49 | 67 | +18 |
| Endpointy | 19 | 21 | +2 (/refresh, /logout) |
| DB tabulky | 8 | 9 | +1 (refresh_tokens) |
| Zdrojový kód (řádky) | ~1 850 | ~2 188 | +338 |
| Testový kód (řádky) | ~390 | ~512 | +122 |
| Upravené soubory | — | 24 | — |
| Nové soubory | — | 7 | — |

---

## Provedené změny

### Fáze 1: Oprava chyb (audit findings)

| ID | Závažnost | Popis | Stav |
|---|---|---|---|
| H1 | HIGH | Audit logging neaktivní | Opraveno — wired do auth, chat, screening |
| H3 | HIGH | Chybí security headers | Opraveno — CSP, HSTS (prod), Permissions-Policy |
| H4 | HIGH | GDPR deletion nemazala audit_logs | Opraveno — přidáno mazání audit_logs + refresh_tokens |
| H5 | HIGH | Password invalidation "REDACTED" | Opraveno — `"!"` (Unix locked-account konvence) |
| M1 | MEDIUM | Nepoužité importy | Opraveno — rag.py, audit_log.py |
| M2 | MEDIUM | Nepoužitá třída MessageResponse | Opraveno — odstraněna |
| M3 | MEDIUM | Anthropic client pooling | Opraveno — lazy singleton pattern |
| M5 | MEDIUM | CORS double-https bug | Opraveno — origins z env se používají as-is |
| M6 | MEDIUM | AUDIT Q9/Q10 validace | Opraveno — set membership místo range check |
| M7 | MEDIUM | Anonymní uživatel lockout | Vyřešeno — refresh tokeny umožňují obnovu bez emailu |

### Fáze 2: Alembic migrace

- Inicializace s async template
- `alembic/env.py` — import všech 9 modelů, pgvector extension, settings.database_url
- `alembic.ini` — sqlalchemy.url nastaveno programaticky
- Migrace se generují po připojení k PostgreSQL (by design — žádná DB v dev prostředí)

### Fáze 3: Refresh tokeny

- Nový model `RefreshToken` (SHA-256 hashed, 30d expiry, rotation)
- Security funkce: `create_refresh_token()`, `verify_refresh_token()`, `revoke_refresh_token()`
- JWT access token expiry snížen z 24h na 1h
- Nové endpointy: `POST /auth/refresh`, `POST /auth/logout`
- Token rotation: starý token se revokuje, vydá se nový pár (access + refresh)

### Fáze 4: Rate limiting

- Nový modul `src/core/rate_limit.py` — slowapi s JWT/IP key extraction
- Limity: auth 5/min, refresh 10/min, chat 20/min, tracking+screening 60/min
- In-memory storage (Redis-swappable)

### Fáze 5: Testy (+18 nových)

| Soubor | Testů | Pokrytí |
|---|---|---|
| `tests/test_auth.py` | 7 | Refresh token CRUD, expirace, revokace, rotace |
| `tests/test_rate_limit.py` | 3 | JWT extrakce, IP fallback, nevalidní JWT |
| `tests/test_middleware.py` | 5 | CSP, HSTS (prod/dev), Permissions-Policy, basic headers |
| `tests/test_screening.py` | +3 | Q9/Q10 validace ({0,2,4} vs. {1,3}) |

### Fáze 6: Dokumentace

- `CLAUDE.md` — aktualizované počty, backend tree, stav implementace
- `docs/API_REFERENCE.md` — refresh/logout endpointy, token flow, rate limiting tabulka
- `.env.example` — JWT_EXPIRATION_HOURS=1, REFRESH_TOKEN_EXPIRY_DAYS=30
- `pyproject.toml` — slowapi>=0.1.9

---

## Nové soubory

| Soubor | Účel |
|---|---|
| `src/core/rate_limit.py` | Rate limiting konfigurace (slowapi) |
| `src/models/refresh_token.py` | RefreshToken ORM model |
| `tests/test_auth.py` | Testy refresh tokenů |
| `tests/test_rate_limit.py` | Testy rate limit key function |
| `tests/test_middleware.py` | Testy security headers |
| `alembic/env.py` | Alembic async migration environment |
| `alembic.ini` | Alembic konfigurace |

---

## Verifikace

- **Testy:** 67/67 PASS (0.66s)
- **Lint (ruff):** 29 warningů — všechny pre-existující (české řetězce v screening.py, StrEnum v crisis.py). Žádné nové.
- **Konzistence:** Endpoint count opraveno (21, ne 22). Modely v alembic/env.py odpovídají src/models/. Všechny importy čisté.

---

## Známé limity a zbývající práce

### Blokující pro production
1. **Clinical advisor review** — crisis.py a guardrails.py potřebují klinické schválení
2. **Legal advisor review** — GDPR Article 9 compliance (zdravotní data)
3. **PostgreSQL migrace** — `alembic upgrade head` po nasazení DB
4. **Frontend** — React Native zatím neexistuje

### Technický dluh (nízká priorita)
- Pre-existující E501 v screening.py (české/anglické texty) — nelze zkrátit bez ztráty čitelnosti
- Pre-existující UP042 v crisis.py (StrEnum) — safety-critical modul, záměrně ponecháno
- `passlib` DeprecationWarning pro `crypt` modul (Python 3.13) — passlib upstream issue

### Doporučení pro další session
1. Generovat Alembic migrace (vyžaduje PostgreSQL)
2. Nasadit na staging (EU region)
3. Implementovat frontend (React Native)
4. Přidat integrační testy s reálnou DB
5. Load testing rate limitů

---

## Bezpečnostní stav

| Vrstva | Implementace | Stav |
|---|---|---|
| Autentizace | JWT (1h) + refresh (30d, SHA-256) | Hotovo |
| Autorizace | User-scoped queries, FK constraints | Hotovo |
| Rate limiting | slowapi (JWT/IP key, per-endpoint limity) | Hotovo |
| Šifrování | AES-256-GCM (konverzace, zprávy) | Hotovo |
| GDPR erasure | 7 tabulek + password invalidation | Hotovo |
| Security headers | CSP, HSTS, X-Frame-Options, X-Content-Type | Hotovo |
| Audit trail | AuditLog model, AI Act compliance | Hotovo |
| Crisis detection | Zero-dep, pre-LLM, CZ+EN keywords | Hotovo |
| Output guardrails | Post-LLM filter (diagnózy, léky) | Hotovo |
| CORS | Env-aware (dev: localhost, prod: z env) | Hotovo |
| Input sanitization | Zero-width char stripping, LIKE injection fix | Hotovo |

---

*Report vygenerován automaticky jako součást manažerské kontroly Session 1.*
